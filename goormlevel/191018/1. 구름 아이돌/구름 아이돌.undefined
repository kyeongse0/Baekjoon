N = int(input())
scores = list(map(int, input().split()))

# (점수, 지원자 번호) 리스트 생성
candidates = [(score, i+1) for i, score in enumerate(scores)]

# 점수 기준 내림차순 정렬
candidates.sort(key=lambda x: x[0], reverse=True)

# 상위 3명의 지원자 번호 추출
top_3 = [str(c[1]) for c in candidates[:3]]

print(" ".join(top_3))
